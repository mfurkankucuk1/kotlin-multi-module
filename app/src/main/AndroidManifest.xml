<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:name="tr.mfk.multimodule.MultiModuleApplication"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MultiModule"
        tools:targetApi="31">
        <activity
            android:name="tr.mfk.multimodule.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="tr.mfk.ecommerce.ui.view.ECommerceActivity"
            android:enabled="true"/>

        <activity
            android:name="tr.mfk.noteapp.view.NoteActivity"
            android:enabled="true"/>

    </application>


</manifest>